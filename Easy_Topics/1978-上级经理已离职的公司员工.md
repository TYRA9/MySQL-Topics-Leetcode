#### **No.1978-上级经理已离职的公司员工**:
1. **力扣链接**：https://leetcode.cn/problems/employees-whose-manager-left-the-company/
2. **题目说明**
    - //todo
3. **答案**（纯代码）： 
      
    ```sql
    # Write your MySQL query statement below
    # Method One: LEFT JOIN
    SELECT
        `E1`.`employee_id`
    FROM 
        `Employees` `E1`
    LEFT JOIN
        `Employees` `E2` ON `E1`.`manager_id` = `E2`.`employee_id`
    WHERE
        (`E1`.`manager_id` IS NOT NULL) AND 
        (`E2`.`employee_id` IS NULL) AND
        (`E1`.`salary` < 30000)
    ORDER BY
        `employee_id`;

    # ------------------------------
    # Method Two: NOT IN
    SELECT
        `employee_id`
    FROM
        `Employees`
    WHERE
        `salary` < 30000 AND 
        `manager_id` IS NOT NULL AND
        `manager_id` NOT IN (
            SELECT `employee_id` FROM `Employees`
        )
    ORDER BY
        `employee_id`;

    # ------------------------------
    # Method Three: NOT EXISTS
    SELECT
        `employee_id`
    FROM
        `Employees` `E1`
    WHERE
        `salary` < 30000 AND
        `manager_id` IS NOT NULL AND
        NOT EXISTS (
            SELECT 1
            FROM `Employees` `E2`
            WHERE `E2`.`employee_id` = `E1`.`manager_id`
        )
    ORDER BY
        `employee_id`;
    ```
4. **思路及解析**：
    - //todo