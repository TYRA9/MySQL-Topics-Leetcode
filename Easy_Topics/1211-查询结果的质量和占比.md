#### **No.1211-查询结果的质量和占比**:
1. **力扣链接**：https://leetcode.cn/problems/queries-quality-and-percentage/description/
2. **题目说明**
    - //todo
3. **答案**（纯代码）： 
      
    ```sql
    # Write your MySQL query statement below
    # Method One: GROUP BY, AVG, SUM / COUNT
    SELECT
        `query_name`,
        ROUND(AVG(`rating` / `position`), 2) AS `quality`,
        ROUND(SUM(IF(rating < 3, 1, 0)) / COUNT(*) * 100, 2) AS `poor_query_percentage`
    FROM
        `Queries`
    GROUP BY
        `query_name`;

    # -------------------------
    # Method Two: GROUP BY, AVG
    SELECT
        `query_name`,
        ROUND(AVG(`rating` / `position`), 2) AS `quality`,
        ROUND(AVG(`rating` < 3) * 100, 2) AS `poor_query_percentage`
    FROM
        `Queries`
    GROUP BY
        `query_name`;
    ```
4. **思路及解析**：
    - //todo