#### **No.1633-各赛事的用户注册率**:
1. **力扣链接**：https://leetcode.cn/problems/percentage-of-users-attended-a-contest/description/
2. **题目说明**
    - //todo
3. **答案**（纯代码）： 
      
    ```sql
    # Write your MySQL query statement below
    --  In MySQL, you cannot wrap one aggregate function (like COUNT) inside another (like SUM).
    # Method Two : Single Table Using SubQuery in SELECT Clause
    SELECT
        `contest_id`,
        ROUND(COUNT(`user_id`) * 100 / (SELECT COUNT(*) FROM `Users`), 2) AS `percentage`
    FROM
        `Register`
    GROUP BY
        `contest_id`
    ORDER BY
        `percentage` DESC,
        `contest_id` ASC;
    ```
4. **思路及解析**：
    - //todo